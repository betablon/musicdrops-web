# Local Claude Code preview / harness config
.claude/

# macOS file-system noise
**/.DS_Store

# Local-only image storage (drafts, references, never shipped)
images/local/

# App Store screenshot helper (local dev tool, not shipped)
appstore-screenshot-notifications.html

# OG card render templates (local dev tools, not shipped)
og-card-template.html
og-card-subpage-template.html
